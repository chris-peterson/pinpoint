{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "case \"$CLAUDE_FILE_PATH\" in */src/pinpoint/*) echo 'REMINDER: Source file changed. If this changes a behavior or convention the system guarantees, consider updating SPEC.md.' ;; esac"
          }
        ]
      }
    ]
  }
}
