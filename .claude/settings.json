{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "case \"$CLAUDE_FILE_PATH\" in */implementations/*) echo 'REMINDER: Implementation file changed. If this is specific to this implementation, consider updating DECISIONS.md. If it defines a behavior/convention for all implementations, consider updating SPEC.md.' ;; esac"
          }
        ]
      }
    ]
  }
}
